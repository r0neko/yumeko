const PacketParameterCollection = require("../utils/packet_parameter_collection");

module.exports = (packet, conn) => {
    //let p = PacketParameterCollection.Parse(packet.data);
    //console.log("debug info:", p.Get("211").toString());
};